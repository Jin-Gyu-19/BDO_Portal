# 추후 할 일

## 1. AI 칼 — 실제 AI처럼 답하게 하기 (API 없이)
지금은 키워드별 준비된 답변 + "리뷰함 열어줘" 같은 앱 실행 의도만 처리합니다. 답변 함수를 `askKal(text)` 하나로 분리해 두면 아래 어느 방식으로도 갈아끼울 수 있습니다.

| 순서 | 방식 | 되는 곳 | 비고 |
|---|---|---|---|
| ① 추천 | **포털 데이터를 아는 규칙형 비서** — 프로젝트·마감·공지·메모·앱을 직접 조회해 답함 ("알파테크 진행률?", "이번 주 마감?", "박회계 담당 건", 날짜·D-day 계산) | 아티팩트·모바일·사내망 모두 | 외부 의존 없음, 틀리지 않음, 데이터 밖 질문엔 "모르겠다" |
| ② | **인터넷 정보** — 키 없이 브라우저에서 부를 수 있는 공개 소스: 위키백과 요약, 날씨(Open-Meteo), 환율(Frankfurter), 공휴일 | GitHub Pages·사내 서버에 올린 파일에서만 | 클로드 아티팩트 뷰어는 외부 요청을 막아 동작 안 함. 일반 웹검색은 키 없이는 불가 |
| ③ | **브라우저 안 소형 모델(WebLLM)** — Qwen 0.5B~1.5B 등을 WebGPU로 실행 | 데스크톱 크롬·엣지, 호스팅 필요 | 첫 다운로드 0.5~1.5GB, 모바일 어려움, 한국어 품질은 "그럴듯하지만 종종 틀림". "AI 모드 켜기" 토글로 선택 사항으로 |

### ③ WebLLM 보안 검토 메모
- **데이터 유출**: 추론이 사용자 PC 안에서만 일어나 질문·조서·메모가 밖으로 나가지 않음. 클라우드 API보다 안전. 모델을 한 번 받으면 오프라인 동작.
- **공급망**: 모델 파일·라이브러리를 외부(HuggingFace·CDN)에서 받으면 변조 위험 → 사내 서버에 복사해 버전·해시 고정, 외부 다운로드 제거.
- **프롬프트 인젝션**: 공지·메모 본문에 숨은 지시가 모델을 흔들 수 있음 → 앱 실행 같은 **행동은 지금처럼 규칙 코드가 판단**, 모델은 문장 생성만. 모델에 실행·삭제 권한을 주지 않음.
- **환각**: 소형 모델은 K-IFRS 조항·기준서 내용을 지어냄 → "참고용 · 기준서 원문 확인 필요" 상시 표시, 수치·마감·프로젝트 등 사실은 포털 데이터에서.
- **기기 캐시**: 모델(1GB 안팎)이 브라우저 캐시에 남음. 공용 PC 용량·정리 정책 필요(업무 데이터는 포함되지 않음).
- **라이선스**: Qwen은 Apache 2.0(상업 이용 가능), Gemma·Llama는 별도 약관 확인.

### 실행 계획
1. ①을 구현하고 `askKal(text)`로 분리
2. 호스팅 환경이 생기면 ②(위키·날씨·환율) 추가
3. ③은 데모 PC의 WebGPU 지원을 확인한 뒤 토글로 추가. 실제 도입 시엔 법인 내부 LLM API를 같은 함수에 연결
