# 설계안 관리 규칙

작업 파일은 항상 루트의 `sample-21-ipad-window-os.html` 입니다. 특정 시점의 설계를 "안"으로 고정할 때 `plans/` 아래에 스냅샷을 복사해 둡니다.

| 안 | 폴더 | 뜻 |
|---|---|---|
| **A안** | `plans/A/` | 기준 설계. 이후 모든 발전은 A안에서 출발 |
| A-1안 | `plans/A-1/` | A안에서 발전시킨 첫 번째 안 |
| A-2안, A-3안 … | `plans/A-2/`, `plans/A-3/` … | 같은 방식으로 이어짐 |

## 규칙
- `plans/*/` 안의 파일은 스냅샷이므로 **수정하지 않습니다.** 고칠 것이 있으면 작업 파일에서 고친 뒤 새 안으로 저장합니다.
- 각 안 폴더에는 `README.md`(저장일 · 기준 커밋 · 이 안에서 달라진 점)와 HTML 한 벌을 둡니다. HTML은 아이콘·로고를 모두 내장해서 파일 하나로 열립니다.
- 새 안을 저장할 때는 "A-N안으로 저장해줘"라고 하면 됩니다. 이전 안과의 차이를 README에 적습니다.
- 갤러리(`index.html`)의 21번 카드는 항상 **현재 작업 파일**을 가리킵니다.

## 현재 상태
- A안: 저장됨 (`plans/A/README.md`)
- A-1안: 번호를 건너뛰었습니다 (사용하지 않음)
- A-2안: 저장됨 — A안을 깔끔하게 다듬은 안 (`plans/A-2/README.md`)
- A-3안: 저장됨 — 칼 검색 홈 (`plans/A-3/README.md`). 작업 파일의 기본 홈 스타일이 '칼 검색'이며 프로필 메뉴에서 A-2 대시보드로 되돌릴 수 있음
- 다음 발전 작업은 A-4안이 됩니다.
